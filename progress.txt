2026-05-19  Step 1 (Infra): wrote docker-compose.yml, .env.example, drizzle.config.ts; added deps (drizzle-orm, drizzle-kit, drizzle-zod, postgres, zod, zod-to-json-schema, date-fns to core; hono+zod to api; commander to cli; @tanstack/react-query, react-router, tailwind, vite, @vitejs/plugin-react, postcss, autoprefixer, date-fns to web). bun install + bun run typecheck + docker compose config all clean.
2026-05-19  Step 2 (DB schema + first migration): wrote packages/core/src/env.ts, db/schema.ts, db/client.ts, db/migrate.ts; dropped greet() and updated stub api/cli/web entries; added drizzle-kit/drizzle-orm/postgres at root devDeps so drizzle-kit can resolve; remapped compose 5432→5435 (5432 occupied by another project's container); generated packages/core/drizzle/0000_loving_lyja.sql; ran migrator against miel-postgres — verified 8 tables (accounts, app_settings, labels, message_labels, messages, suggested_labels, triage_label_suggestions, triages) + 2 enums (priority, suggestion_status). bun run typecheck clean.
2026-05-19  Step 3 (gog adapter + gmail zod schemas): wrote packages/core/src/adapters/shell.ts (Bun.spawn wrapper with spawnCapture/spawnJson/spawnVoid + ShellError), packages/core/src/schemas/gmail.ts (GogAccount, GogAuthList, GogLabel, GogSearchHit/Response, recursive GogMessagePayload + GogMessageRaw, GogMessageDecoded union, GogCreateLabelResponse, GogSendResponse), packages/core/src/util/gmailPayload.ts (base64url decode, walk payload tree → bodyText/bodyHtml, extractHeaders, parseFromHeader, parseAddressList, parseInternalDate), and packages/core/src/adapters/gog.ts (typed GogAdapter with listAccounts/searchMessages/getMessage/listLabels/createLabel/batchModifyLabels/trashThread/archiveThread/sendReply, shelling out to GOG_BIN with --json + zod-validated outputs). Re-exported from @miel/core. Smoke test: createGogAdapter().listAccounts() returned both authorized gog accounts through the zod schema. searchMessages/getMessage adapter wired but local OAuth token is invalid_grant — to be exercised once token refresh is done. bun run typecheck clean.
2026-05-19  Step 4 (claude adapter + triage/reply zod schemas + settings service): wrote packages/core/src/schemas/triage.ts (Priority, TriageInputItem, TriageInput, SuggestedNewLabel, TriageOutputItem, TriageOutput with min(1)/max(20) batch + name length cap), packages/core/src/schemas/reply.ts (ReplyGenInput, ReplyGenOutput), packages/core/src/services/settings.ts (SETTING_KEYS, SETTING_DEFAULTS — triage.model=claude-haiku-4-5, reply.model=claude-sonnet-4-6 — plus getSetting/setSetting/getModelSettings/updateModelSettings using drizzle onConflictDoUpdate on app_settings), and packages/core/src/adapters/claude.ts (createClaudeAdapter exposing runTriage/generateReply; reads model from settings; spawns CLAUDE_BIN -p --output-format=json --model=<m> --json-schema=<json> <prompt> via spawnCapture; unpacks outer envelope → result string → JSON.parse → zod parse; returns {output, runId, model}; surfaces is_error envelopes via ShellError). zodToJsonSchema(TriageOutput|ReplyGenOutput) emits draft-07 JSON that --json-schema accepts. Re-exported triageSchemas, replySchemas, createClaudeAdapter, and settings helpers from @miel/core. Smoke test against live miel-postgres: defaults read (haiku/sonnet), updateModelSettings({triageModel: sonnet}) → read back → reset to haiku all round-trip; fake TriageOutput + ReplyGenOutput parse cleanly. Typecheck clean. Live `claude -p` invocation deferred to first sync run.
2026-05-19  Step 5 (sync service): wrote packages/core/src/util/time.ts (parseSince → newer_than:<window>, validates d|h|w|m|y suffix), packages/core/src/services/accounts.ts (syncAccountsFromGog inserts new account rows from gog auth list, returns {id, email, displayName, inserted}; getAccountByEmail helper), packages/core/src/services/labels.ts (syncLabelsForAccount bulk upsert with excluded.name/excluded.type, getLabelsForAccount, getLabelsByGmailIds, ensureLabel that calls gog.createLabel when local row missing), and packages/core/src/services/sync.ts (fetchAndTriage + syncAll). Pipeline: sync labels → search messages (gog.searchMessages with newer_than query, default max=200) → getMessage per hit → normalize via either GogMessageDecoded shape or raw payload walk (extractBodies/extractHeaders/parseFromHeader/parseAddressList/parseInternalDate) → upsert messages (excluded.*) + message_labels (onConflictDoNothing, resolved by gmail label id → uuid) → chunk normalized rows into batches of 15 (TRIAGE_BATCH_SIZE), call claude.runTriage with TriageInput {account, existingLabels (user-type names only), messages: items with body truncated to 4000 chars}, match results by id (not index, with warnings logged for missing/extra ids) → for each result write a triages row + triage_label_suggestions rows for applyExistingLabels matched against labelsByName + suggested_labels rows for suggestNewLabels → update accounts.last_synced_at. syncAll wraps fetchAndTriage with optional accountEmail filter and pre-runs syncAccountsFromGog. Re-exported syncAccountsFromGog/syncLabelsForAccount/ensureLabel/fetchAndTriage/syncAll/parseSince from @miel/core. Smoke test against live miel-postgres with stubbed gog (returns 2 messages) + stubbed claude (returns 1 high w/ existing-label match + new-label suggestion, 1 low): result = {fetched:2, triaged:2, suggestedNewLabels:1, errors:[]}; verified 2 messages, 4 message_labels, 2 triages (high+low), 1 triage_label_suggestions row for Newsletters, 1 pending suggested_labels row, accounts.last_synced_at set. Truncated test rows. Typecheck clean across all 5 packages. Live gog/claude exercise deferred to first real CLI run (Step 6).
2026-05-19  Step 6 (CLI commands): wrote packages/core/src/services/apply.ts (applyLabels with gog.batchModifyLabels add/remove + message_labels insert/delete; applySuggestions implementing the PRD's 6-step new-label flow — for existing-label IDs attaches via gog and marks triage_label_suggestions 'applied'; for suggested-label IDs calls ensureLabel which goes through gog.createLabel + labels upsert, then attaches via gog + marks suggested_labels 'applied' with created_label_id pointer; archiveMessage + trashMessage shell out to gog.archiveThread/trashThread and flip messages.is_archived/is_trashed; getLatestTriageForMessage joins triage_label_suggestions+labels+suggested_labels for the most recent triage by createdAt desc) and packages/core/src/services/reply.ts (generateReply resolves message via accounts join, truncates body to 8000 chars, calls claude.generateReply; sendReply derives reply-to recipients from messages.toEmails with fallback to fromEmail and calls gog.sendReply with replyToMessageId). Added closeDb() to db/client.ts so the CLI exits without dangling postgres sockets. Built packages/cli/src/commands/{db,accounts,sync,apply,reply}.ts using commander subcommands and wired them in packages/cli/src/index.ts with showHelpAfterError, finally→closeDb, and exit-code 1 on uncaught errors. Re-exported applyLabels/applySuggestions/archiveMessage/trashMessage/getLatestTriageForMessage/generateReply/sendReply/closeDb from @miel/core. Smoke test (packages/cli/scripts/smoke-cli.ts, stubbed gog + claude, live miel-postgres on 5435): fetchAndTriage → fetched=2 triaged=2 suggestedNewLabels=1 errors=0; getLatestTriageForMessage returned the high triage with 1 pending existing-label suggestion + 1 pending new-label suggestion; applySuggestions accept-all → existing 'Newsletters' attached via gog stub + 'SmokeNew' label created via gog.createLabel + suggested_labels.status='applied' + triage_label_suggestions.status='applied'; generateReply returned {subject:'Re: Smoke', body:'Stubbed reply body.', model:'claude-fake'}; sendReply returned sentMessageId='smoke-sent-1'; archiveMessage + trashMessage both updated DB flags and invoked gog.archiveThread/trashThread; all smoke rows then cleaned (verified via docker exec psql: 0 messages/triages/suggested_labels matching smoke-%). CLI smoke: `miel --help`, `miel accounts list`, `miel accounts sync`, `miel db migrate`, `miel apply --message-id=missing@x.com:nope`, `miel reply --message-id=missing@x.com:nope --prompt=test` all exit with correct status codes (0 for known-good, 1 for unknown account, 0 with "no triage" message for unknown message id). bun run typecheck clean across all 4 packages. Live gog/claude exercise still deferred to first real user-driven sync.
2026-05-19  Step 7 (API server): wrote packages/core/src/schemas/api.ts (zod request/query schemas — SyncRequest, CreateLabelRequest, ListMessagesQuery with cursor/limit/includeArchived/includeTrashed coercion, ModifyLabelsRequest, ApplySuggestionsRequest, GenerateReplyRequest, SendReplyRequest, UpdateSettingsRequest) and packages/core/src/services/messages.ts (listMessages using a WITH latest_triage AS (SELECT DISTINCT ON (account_id, gmail_message_id) … ORDER BY created_at DESC) CTE left-joined to messages, with composite cursor over (internalDate, gmailMessageId), optional label filter via inner join, archived/trashed defaulted excluded; getMessageDetail returning headers/labels/full triage history joined to existing+new label suggestions; listAccountsFromDb/getAccountById). Re-exported apiSchemas/listMessages/getMessageDetail/listAccountsFromDb/getAccountById and the matching types from @miel/core. Built the Hono app: packages/api/src/middleware/auth.ts (Bearer match against API_SECRET → 401 on miss), middleware/error.ts (ZodError→400 validation_failed, ShellError→502 shell_error with stderr/exitCode, HTTPException pass-through, fallback→500 internal_error), routes/{accounts,labels,messages,sync,settings}.ts, and app.ts mounting routes under hono/cors (origin=http://localhost:3000; allowHeaders=Authorization,Content-Type; allowMethods=GET/POST/PUT/DELETE/OPTIONS), with GET /health registered BEFORE bearerAuth so it stays public, then app.use("*", bearerAuth()) gating everything else. Routes: GET /accounts, POST /accounts/sync, GET /accounts/:id/labels, POST /accounts/:id/labels/sync, POST /labels, GET /messages (?account=&priority=&label=&limit=&cursor=&includeArchived=&includeTrashed=), GET /messages/:accountId/:gmailMessageId, POST /messages/:accountId/:gmailMessageId/labels, POST /messages/:accountId/:gmailMessageId/apply-suggestions, POST /messages/:accountId/:gmailMessageId/archive, DELETE /messages/:accountId/:gmailMessageId, POST /messages/:accountId/:gmailMessageId/generate-reply, POST /messages/:accountId/:gmailMessageId/send-reply, POST /sync (tolerant to empty body), GET/PUT /settings. src/index.ts now boots createApp() via Bun.serve({ port: API_PORT, fetch: app.fetch }) and logs the URL. Smoke test (packages/api/scripts/smoke-api.ts, in-process via app.fetch(new Request(...)), seeds 2 messages + 1 high triage with existing-label match + new-label suggestion through fetchAndTriage using stubbed gog+claude, live miel-postgres on 5435): no-auth /accounts→401 unauthorized, /health (no auth)→200 {ok:true}, /accounts→2 accounts, /accounts/:id/labels→2 labels, /messages?priority=high→1 item (smoke-api-msg-1) with attached labels + priority='high' + triageId set, /messages/:accountId/:gmailMessageId→subject + 1-entry triageHistory with existing+new suggestions, /settings→{triageModel:claude-haiku-4-5, replyModel:claude-sonnet-4-6}, PUT /settings {triageModel:claude-sonnet-4-6}→updated round-trip (reset after), invalid priority=URGENT→400 validation_failed (ZodError surfaced), unknown account UUID GET /accounts/.../labels→404 account_not_found. apply-suggestions over HTTP correctly invokes real createGogAdapter() (no stub injection across the wire) and the gog call returns invalid_grant — propagated through the error middleware as 502 shell_error with stderr + exitCode + cmd, verifying ShellError handling. Live gog/claude exercise of archive/apply-suggestions/generate-reply/send-reply over HTTP deferred until OAuth refresh; CLI smoke covers those paths with adapter injection. Live boot: `bun run packages/api/src/index.ts` logs "miel api listening on http://localhost:3001"; `curl /accounts`→401, `curl /health`→200, `curl -H "Authorization: Bearer test-secret" /accounts`→200. bun run typecheck clean across all 4 packages. Reused: hono@4.12.20 already installed in @miel/api (PRD step 1).
2026-05-19  Step 8 (Web shell): wrote packages/web/vite.config.ts (React plugin + dev server port from WEB_PORT + /api proxy → http://localhost:${API_PORT||3001} with rewrite stripping /api), tailwind.config.ts (content scan + custom miel.* palette: bg/panel/ink/muted/line/accent + high/medium/low priority chips), postcss.config.cjs (tailwind+autoprefixer), index.html (root + /src/main.tsx), src/index.css (@tailwind directives + body bg/ink). Bootstrap: src/main.tsx (StrictMode > QueryClientProvider > RouterProvider; QueryClient with staleTime=30s, no refetch-on-focus, retry=1), removed legacy src/index.tsx. Auth-aware fetch: src/config.ts (reads VITE_API_BASE/VITE_API_SECRET, warns if secret missing), src/api/client.ts (apiFetch<T> builds path+search via URL ctor, always sets Authorization: Bearer ${VITE_API_SECRET}, JSON-encodes body, typed ApiError with status+parsed body on non-2xx), src/api/types.ts (Account, Label, Priority, MessageLabel, ListedMessage, ListMessagesResponse, MessageDetail, SyncRunResult, SyncResponse, ModelSettings — match @miel/core's service exports and API JSON), src/api/queries.ts (useAccounts/useLabels(accountId)/useMessages(params)/useMessage(accountId, gmailMessageId)/useSettings — typed react-query hooks keyed under accounts | labels | messages | settings, gated by `enabled` when ids are absent), src/api/mutations.ts (useSync posting {account?, since?} to /sync, invalidating accounts + messages on success). Routing: src/router.tsx createBrowserRouter with App layout + child routes index→InboxPage, messages/:accountId/:gmailMessageId→MessageDetailPage (Step 9 placeholder), settings→SettingsPage (Step 12 placeholder, read-only model display already wired via useSettings). Layout: src/App.tsx owns selectedAccountId/selectedLabelId/syncStatus state, auto-selects first account once useAccounts resolves, threads {selectedAccountId, selectedLabelId} via <Outlet context={...}>, renders Sidebar + main column with SyncStatusBanner above the Outlet. Components: src/components/Sidebar.tsx (heading, AccountPicker, SyncButton, scrollable LabelList, Settings NavLink), AccountPicker.tsx (select bound to useAccounts; loading/empty/error states), LabelList.tsx (separates user vs system labels via type !== 'system', "All messages" reset row, click toggles selectedLabelId), Button.tsx (primary|secondary|ghost|danger), Spinner.tsx, EmptyState.tsx, LabelBadge.tsx (existing labels), SuggestedLabelBadge.tsx (existing vs new suggestion styling via kind prop), PrioritySection.tsx (priority chip + count + MessageRow list + empty state), MessageRow.tsx (router Link to detail route, relative time via date-fns/formatDistanceToNow, sender/subject/snippet, INBOX label filtered out, max 6 label badges). Features: src/features/sync/SyncButton.tsx (uses useSync with since=7d, totals fetched/triaged/errors across runs into the layout banner), SyncStatusBanner.tsx (idle/ok/error states with dismiss). Inbox: src/pages/InboxPage.tsx reads selectedAccountId/selectedLabelId via useOutletContext, fetches via useMessages({accountId, labelId, limit: 100}), groups results into byPriority + an untriaged bucket, renders three PrioritySection blocks + Untriaged section, with empty/loading/error fallbacks (and a "pick an account" CTA when no account is selected). src/vite-env.d.ts declares vite/client + ImportMetaEnv so import.meta.env.VITE_* type-checks cleanly. Verified: bun run typecheck clean across all 4 packages (web cache miss → success); `cd packages/web && bun run build` → vite production build 411 modules, 11.10 kB CSS, 352.25 kB JS gzip 111.10 kB. Live boot smoke: API booted on :3001 (DATABASE_URL=postgres://miel:miel@localhost:5435/miel API_SECRET=test-secret), `bun run dev` from packages/web on :3000 with VITE_API_BASE=/api VITE_API_SECRET=test-secret — `curl :3000/` → 200 wired-up index, `curl :3001/health` → {"ok":true} 200, `curl :3000/api/health` → {"ok":true} 200 (proxy works), `curl :3000/api/accounts` no-auth → {"error":"unauthorized"} 401, `curl -H "Authorization: Bearer test-secret" :3000/api/accounts` → 200 with both real accounts (lucasriondelpro@gmail.com + lucasrndl@gmail.com). Detail/Settings pages are placeholder routes (Steps 9-12 will flesh out); only the sync mutation needed for the sidebar Sync button is wired — archive/trash/reply/apply-suggestions/labels/settings PUT mutations are intentionally deferred to their dedicated steps.
2026-05-19  Step 9 (Detail page): wrote packages/web/src/features/message-detail/MessageDetailHeader.tsx (subject + latest-triage priority chip via priorityChipStyles record, or "untriaged" pill when triageHistory[0] is missing; From/To/Date(date-fns format(PPpp))/Account dl; existing-labels row with INBOX gmailLabelId filtered out, rendered via LabelBadge), MessageDetailBody.tsx (HTML/Text toggle defaults to "html" when bodyHtml exists else "text"; toggle buttons only render when both bodies exist; HTML renders inside `<iframe title="message-body" sandbox="" srcDoc={bodyHtml}>` — empty sandbox attribute disables scripts/forms/popups/same-origin per PRD requirement; text renders in `<pre>` with whitespace-pre-wrap; falls back to "No body content available" empty-state when both bodies are blank), SuggestedLabelsPanel.tsx (reads triageHistory[0]; renders separate "Existing labels" + "New labels" sections each as a list of SuggestedLabelBadge + status pill via statusStyles record covering pending/applied/dismissed; "not been triaged yet — run a sync" copy when no triage exists; "Claude did not suggest any labels" copy when triage exists with empty arrays; header shows the triage id slice for traceability), TriageHistoryPanel.tsx (renders message.triageHistory as an ordered list of left-border-2 entries, each with priority chip + date-fns timestamp + optional model + reasoning; empty state when triageHistory is empty). Rewrote packages/web/src/pages/MessageDetailPage.tsx to call useMessage(accountId, gmailMessageId) from useParams, render Loading (Spinner + "Loading message…") / Error (EmptyState with ApiError message) / Success states; success layout is grid-cols-1 lg:grid-cols-[1fr_320px] with MessageDetailHeader + MessageDetailBody on the left and SuggestedLabelsPanel + TriageHistoryPanel as an aside on the right; "← Back to inbox" link to "/" persists across all states. Read-only — no mutations wired (Step 10 will add archive/trash, Step 11 will add reply composer, apply-suggestions also deferred). Verified: bun run typecheck → clean across all 4 packages (web cache miss → success after the 4 new files + 1 rewrite); `cd packages/web && bun run build` → vite production build 416 modules, 12.02 kB CSS (up from 11.10 kB), 372.92 kB JS gzip 115.54 kB (up from 352.25 kB / 111.10 kB). Live browser exercise deferred — the new components consume the MessageDetail JSON shape that was already validated end-to-end in Step 7's smoke test (subject + triageHistory with existing+new suggestions + labels round-trip through the API).
2026-05-19  Step 11 (Reply composer): extended packages/web/src/api/mutations.ts with useGenerateReply (POST /messages/:accountId/:gmailMessageId/generate-reply, body {prompt}, returns {subject, body, model, runId}) and useSendReply (POST /messages/:accountId/:gmailMessageId/send-reply, body {subject, body}, returns {ok, sentMessageId}; onSuccess invalidates ["messages"] + queryKeys.message(accountId, gmailMessageId) so both the inbox list and the detail page reflect the sent reply). Both mutations key on the same MessageActionInput {accountId, gmailMessageId} shape used by archive/trash so the call sites compose cleanly. Wrote packages/web/src/features/reply/ReplyDraftView.tsx: stateless editable draft — subject `<input>` + body `<textarea rows=12>` bound to controlled-prop state from the parent, primary Send Button + ghost Discard Button, fields/buttons disable while send is pending OR after sentMessageId is set, send-error display below the action row using a local describeError(ApiError|Error|unknown) fallback, model footer chip in the header, "Sent. Gmail message id: …" success state replacing the action row once the send resolves. Wrote packages/web/src/features/reply/ReplyComposer.tsx: section component that owns the prompt `<textarea rows=3>` + draft state + sentMessageId state. Generate flow: useGenerateReply.mutate({...input, prompt}) → onSuccess sets draft={subject, body, model}; button label flips from "Generate" to "Regenerate" once a draft exists; canGenerate gates on trimmed prompt + !isPending; inline "Asking Claude for a draft…" hint shows during pending. Send flow: useSendReply.mutate({...input, subject: draft.subject, body: draft.body}) → onSuccess sets sentMessageId (which ReplyDraftView reads to switch into its sent-confirmation mode). Discard handler resets draft + sentMessageId AND calls send.reset()/generate.reset() so stale errors clear. Wired `<ReplyComposer message={messageQuery.data} />` into pages/MessageDetailPage.tsx after MessageDetailBody inside the left column of the grid layout. Per CLAUDE.md (multiple small files, one component per file), ReplyDraftView and ReplyComposer live in separate files under features/reply/. Verified: bun run typecheck → clean across all 4 packages (web cache miss → success after the 2 new files + 2 file edits); cd packages/web && bun run build → vite production build 419 modules (up from 417 in Step 10), 12.22 kB CSS (up from 12.02), 378.50 kB JS gzip 116.69 kB (up from 374.43 / 115.87). Live browser exercise of generate-reply / send-reply over HTTP deferred — both routes were already smoke-tested in Step 7's smoke-api.ts (generate-reply via the stubbed claude that the API layer can't inject across the wire would itself fail with invalid_grant on send-reply; the CLI smoke covered the happy path with adapter injection). The new mutations only translate the existing JSON shape into react-query side effects. apply-suggestions remains intentionally unwired in the UI; Step 12 (Settings page — ModelPicker, accounts manager, labels manager, sync trigger) and Step 13 (Polish — loading/empty/error states, root bun dev via Turbo running api+web) are the remaining work.
2026-05-19  Step 10 (Actions): extended packages/web/src/api/mutations.ts with useArchiveMessage (POST /messages/:accountId/:gmailMessageId/archive, no request body — the API route does not call c.req.json() — returns { ok: true, threadId }) and useTrashMessage (DELETE /messages/:accountId/:gmailMessageId). Both take MessageActionInput = { accountId, gmailMessageId }, and on success invalidate two query scopes: the broad ["messages"] list cache (so the inbox drops the row, since the SQL listMessages CTE excludes archived/trashed by default) AND queryKeys.message(accountId, gmailMessageId) (so the detail page's isArchived/isTrashed flags refresh if the user stays on it). Wrote packages/web/src/features/message-detail/MessageActions.tsx — two Buttons (Archive→secondary variant, Trash→danger variant) wired to the new mutations. Archive button is disabled when message.isArchived is already true (label flips to "Archived"); Trash button is disabled when message.isTrashed is already true (label flips to "Trashed"); both buttons mutually disable each other while either mutation is pending. Trash gates the call behind a native confirm("Move this message to trash?") prompt (PRD calls out that trash hits Gmail immediately, so the confirmation prevents accidental destructive action). Success handlers navigate back to "/" via useNavigate() so the user lands on the inbox where the now-archived/trashed row has dropped out of the default listMessages filter. Per-action Spinner shows during the pending state; errors render below the button row as red text using ApiError.message via a describeError helper that falls back to "Unknown error" for non-Error throwables. Wired MessageActions into pages/MessageDetailPage.tsx between MessageDetailHeader and MessageDetailBody so the actions are visible above the body content. Verified: bun run typecheck → clean across all 4 packages (web cache miss → success with the new files); `cd packages/web && bun run build` → vite production build 417 modules (up from 416), 12.02 kB CSS, 374.43 kB JS gzip 115.87 kB (up from 372.92 kB / 115.54 kB in Step 9). Live browser exercise deferred — archive/trash routes were already smoke-tested over HTTP in Step 7's smoke-api.ts (verifying both happy-path JSON shape and ShellError fallback via the real createGogAdapter()), and the new mutations only translate the same JSON shape into react-query side effects. apply-suggestions remains intentionally unwired in the UI (no PRD step assigns it to the detail page; Step 11 will add reply composer, Step 12 will surface label management in Settings).
2026-05-19  Step 13 (Polish): wired sync errors from the sidebar SyncButton through SyncStatusBanner — added onError prop to packages/web/src/features/sync/SyncButton.tsx that calls describeError(ApiError|Error|unknown) inside react-query's mutation.onError, threaded it through packages/web/src/components/Sidebar.tsx (onSyncError prop) and packages/web/src/App.tsx so failures now flip syncStatus into {kind:'error', message} instead of being swallowed. Loading/empty/error states elsewhere were already shipped per-step (InboxPage: pick-an-account/loading/error/no-messages; AccountPicker: loading/error/no-accounts; LabelList: loading/error/empty; PrioritySection: per-bucket empty state; MessageDetailPage: loading/error fallbacks; SettingsPage: loading/error for the settings query; SettingsSyncTrigger: inline error + per-run summary; ModelPicker/AccountsManager/LabelsManager: inline ApiError surfaces). Root `bun dev` via Turbo: switched root package.json's `dev` script from `turbo run dev` to `turbo run dev --filter=@miel/api --filter=@miel/web` so @miel/cli's `dev` (which would exit immediately with `bun run src/index.ts` printing help) doesn't get pulled into the persistent task graph. Verified via `bun run turbo run dev --filter=@miel/api --filter=@miel/web --dry`: resolves to exactly @miel/api#dev (`bun run --hot src/index.ts`, persistent) and @miel/web#dev (`vite`, framework=vite, persistent), each with cache:false and no dependents. bun run typecheck clean across all 4 packages (web cache miss → success; core/cli/api cache hits); cd packages/web && bun run build → vite production build 423 modules / 12.83 kB CSS / 389.62 kB JS gzip 118.89 kB. End-to-end happy path documented in the PRD's Verification section is now reachable via the single root command `bun dev` from /Users/lucas/miel.
2026-05-19  Step 12 (Settings page): extended packages/web/src/api/mutations.ts with four new mutations — useUpdateSettings (PUT /settings, onSuccess uses qc.setQueryData(queryKeys.settings, …) to update the cache in place rather than triggering a refetch), useSyncAccounts (POST /accounts/sync, invalidates queryKeys.accounts), useSyncAccountLabels (POST /accounts/:accountId/labels/sync, invalidates queryKeys.labels(accountId)), and useCreateLabel (POST /labels with {accountId, name}, invalidates queryKeys.labels(accountId)). Wrote four feature components under packages/web/src/features/settings/: ModelPicker.tsx (per-task model selector — props `task: 'triage' | 'reply'`, `label`, `description`, `value`, optional `onSaved`; renders a curated `<select>` of claude-haiku-4-5 / claude-sonnet-4-6 / claude-opus-4-7 alongside a free-text input so power users can paste a custom model id; when the typed value isn't one of the presets the select shows a `__custom__` sentinel option labeled "Custom: <value>"; Save button is disabled until the draft differs from the prop value AND the trimmed draft is non-empty AND no mutation is in-flight; calls useUpdateSettings with the per-task payload — {triageModel} or {replyModel} — and surfaces "Saved" inline + ApiError via a local describeError fallback). AccountsManager.tsx (reads useAccounts; renders email + optional displayName + lastSyncedAt as "Last synced X ago (full timestamp via date-fns format(PPpp))" or "Never synced"; "Import from gog" button calls useSyncAccounts so the user can re-pull the authorized account list after running `gog auth add` in a terminal; EmptyState when accounts.data is empty; Spinner during load; ApiError surfaced inline). LabelsManager.tsx (account `<select>` defaults to first useAccounts entry via `effectiveAccountId = selectedAccountId ?? accounts.data?.[0]?.id`; useLabels(effectiveAccountId) splits results into user-type and system-type LabelGroup clusters of LabelBadges; "Sync labels" button calls useSyncAccountLabels to pull fresh labels from Gmail via gog; inline create form — text input + "Create label" button — calls useCreateLabel which hits the API's ensureLabel path (gog.createLabel + DB upsert) and resets the input on success; EmptyStates for "No account selected" and "No labels yet"). SettingsSyncTrigger.tsx (richer sync widget than the sidebar's SyncButton: account picker with "All accounts" sentinel, window picker covering 1d/7d/30d/90d, "Sync now" button calls useSync({account?, since}) and stores the returned SyncRunResult[] in local state; renders a per-run summary block underneath listing fetched/triaged/suggestedNewLabels/errors counts per account; resets the summary on each new request to avoid stale display). Rewrote packages/web/src/pages/SettingsPage.tsx to compose the four sections (Claude models / Sync / Accounts / Labels) under a shared header + back-to-inbox link, with section labels styled as small-caps uppercase headings (text-sm font-semibold uppercase tracking-wide text-miel-muted); settings query's loading state shows Spinner + "Loading settings…", error state renders an EmptyState with the parsed error message via describeError. Verified: bun run typecheck → clean across all 4 packages (web cache miss → success after the 4 new files + 1 mutations edit + SettingsPage rewrite); cd packages/web && bun run build → vite production build 423 modules (up from 419 in Step 11), 12.83 kB CSS (up from 12.22), 389.42 kB JS gzip 118.82 kB (up from 378.50 / 116.69). Live HTTP smoke (boot packages/api/src/index.ts against miel-postgres 5435 with API_SECRET=test-secret): GET /health → 200 {"ok":true}, GET /settings → {triageModel:claude-haiku-4-5, replyModel:claude-sonnet-4-6}, PUT /settings {triageModel:claude-sonnet-4-6} → {triageModel:claude-sonnet-4-6, replyModel:claude-sonnet-4-6} round-trip, then restored to haiku, GET /accounts returned both authorized accounts (lucasriondelpro@gmail.com with lastSyncedAt set, lucasrndl@gmail.com with lastSyncedAt null), GET /accounts/:id/labels returned the seeded INBOX (system) + Newsletters (user) labels — confirming every endpoint the new SettingsPage consumes. Live browser exercise of the create-label / sync-labels paths over HTTP deferred — both routes flow through createGogAdapter() which still surfaces invalid_grant on the local OAuth token (Step 7 already verified the ShellError middleware path that surfaces this as 502 shell_error). Step 13 (Polish — root `bun dev` via Turbo running api + web, additional loading/empty/error polish) is the only remaining work.
2026-05-21  WS sync streaming Step 1 (event schema + core export): wrote packages/core/src/schemas/syncEvents.ts defining one Zod schema per server→client event (SyncStartedEvent, MailsFetchedEvent, TriageStartedEvent, TriageBatchProgressEvent, FiltersStartedEvent, TriageFinishedEvent, FiltersFinishedEvent, SyncFinishedEvent, SyncErrorEvent) plus the SyncStartMessage refinement (rejects both since+range) shared with the existing apiSchemas.SyncRequest. Composed two discriminated unions on `type`: SyncServerEvent (all 9 server-emitted events) and SyncClientMessage (currently sync.start only). SyncRunResult Zod schema mirrors the SyncRunResult interface in services/sync.ts (account/fetched/triaged/suggestedNewLabels/filtersSynced/suggestedFilters/errors) and is embedded in SyncFinishedEvent.runs. Re-exported as `syncEventSchemas` from packages/core/src/index.ts mirroring the existing apiSchemas pattern. Type-only change — no runtime wiring yet. bun run typecheck clean across all 4 packages (core cache miss → success; api/cli/web caches recomputed). Step 2 (add onEvent to fetchAndTriage/syncAll + emit at labeled points, no parallelism yet) is next.
2026-05-21  WS sync streaming Step 2 (onEvent emission in fetchAndTriage/syncAll, no parallelism): added `onEvent?: (event: SyncServerEventT) => void` to FetchAndTriageOptions and SyncAllOptions in packages/core/src/services/sync.ts, importing the union type from ../schemas/syncEvents. In fetchAndTriage: declared `const emit = opts.onEvent ?? (() => {})` alongside `log`, then emitted at the PRD's labeled points — `mails.fetched` (account, count) right after upsertMessages(normalized) so the row count reflects the post-upsert state; `triage.started` (account, totalBatches) right after `const batches = chunk(normalized, TRIAGE_BATCH_SIZE)`; `triage.batch.progress` (account, batchIndex=i 0-based to match the schema's nonnegative integer, totalBatches, status:'started') immediately before each batch's try block, then `status:'done'` after persistTriageResults succeeds, and `status:'failed'` with the error message inside the existing catch (matches today's behavior: one failed batch doesn't abort the loop, but the WS client now learns about it); `triage.finished` (account, triaged, suggestedNewLabels) after the batch loop completes; `filters.started` (account) immediately before the suggestFiltersForBatch try block; `filters.finished` (account, suggestedFilters=proposals.created) AFTER the catch so it fires even when filter suggestion fails (consistent with how SyncRunResult.suggestedFilters reports 0 on failure rather than being absent). The early-return path (normalized.length === 0) intentionally emits no events between sync.started and sync.finished — `mails.fetched` is documented to fire after upsertMessages, which is skipped in that path; the WS wrapper's sync.finished is the client's signal that the run ended with zero hits. `sync.started` / `sync.finished` / `sync.error` are NOT emitted from fetchAndTriage per the PRD — they belong to the WS wrapper. syncAll forwards opts.onEvent into each fetchAndTriage call alongside opts.log. CLI is behaviorally unchanged: packages/cli/src/commands/sync.ts passes `log` but not `onEvent`, so emit() is the no-op default — log output stays byte-for-byte identical. bun run typecheck clean across all 4 packages (5 cache misses, all green in 2.6s). Step 3 (parallelize triage batches + filter suggest via Promise.allSettled / Promise.all) is next.
2026-05-21  WS sync streaming Step 3 (parallelize triage batches + filter suggest): refactored the per-batch loop in packages/core/src/services/sync.ts into an inner async `runTriageBatch(batch, i): Promise<BatchOutcome>` returning `{ persisted, newLabelSuggestions, errors }` with NO shared mutation — every batch builds its own TriageInput, emits its own triage.batch.progress (started/done/failed), and collects errors into a local array that the reducer below merges back into the run-level `errors[]`. Wrapped the existing filter-suggest try/catch in an inner async `runFilterSuggest(): Promise<{ suggestedFilters, errors }>` that emits filters.started up-front and returns `{ suggestedFilters: 0, errors: ['filterSuggest: …'] }` on throw (preserving today's behavior of reporting 0 on failure). Replaced the sequential `for (i…)` loop with `Promise.allSettled(batches.map(runTriageBatch))` AND ran it concurrently with `runFilterSuggest()` via `const [batchResults, filterResult] = await Promise.all([batchPromise, filterPromise])`. After the awaits, reduced `batchResults` (PromiseSettledResult<BatchOutcome>[]) into `triagedCount` + `suggestedNewLabelsCount` + merged `errors`; rejected batches (shouldn't happen since runTriageBatch catches internally, but the type system still requires handling) push a `triage batch N: <msg>` error and log it. `triage.finished` fires after the reducer runs so its counts reflect all completed batches; `filters.finished` fires immediately after with `filterResult.suggestedFilters` (matches the PRD spec — filter result is correctness-safe in parallel because it only depends on `normalized` + `labelsByGmailId`, both ready before the triage chunking step). Removed the now-unused mutable `triagedCount`/`suggestedNewLabelsCount` from the outer scope; the new `let triagedCount=0;` lives in the reducer block. Verified: bun run typecheck → 5 cache misses, all green in 2.6s. CLI smoke (packages/cli/scripts/smoke-cli.ts with stubbed gog+claude, live miel-postgres 5435): fetchAndTriage returns `fetched=2 triaged=2 suggestedNewLabels=1` — byte-for-byte the same totals as the pre-parallel run in Steps 1-2 / Step 5 baseline. The two errors that surface in the smoke run (`syncFilters: gog.listFilters is not a function`, `filterSuggest: claude.runFilterSuggest is not a function`) are pre-existing — the smoke test's stub adapters don't implement those methods, and the parallelism refactor doesn't change which methods are called. Step 4 (WS upgrade in packages/api/src/index.ts + new packages/api/src/ws/syncSocket.ts handler with wscat smoke-test) is next.
2026-05-21  WS sync streaming Step 4 (WS upgrade + syncSocket handler): wrote packages/api/src/ws/syncSocket.ts exposing handleSyncMessage(ws, raw) — guards against double sync.start via ws.data.started, parses the incoming text with try/catch around JSON.parse (emits `sync.error: invalid JSON` on failure) then syncEventSchemas.SyncStartMessage.safeParse (emits `sync.error: invalid sync.start payload` with the formatted zod error on failure). On a valid payload it flips ws.data.started=true, sends `sync.started`, then awaits syncAll({accountEmail, since, range, max, onEvent}) where onEvent forwards every SyncServerEventT through ws.send(JSON.stringify(event)) but only when ws.readyState===1 (OPEN) — so events after a client-side close don't crash the worker. On success sends `sync.finished` with the runs array; on throw sends `sync.error` with err.message; the finally block calls ws.close(1000) so the client always gets a clean termination. The SyncSocketData shape {started, cancelled} lives in this same file and is exported for the Bun.serve generic. Rewrote packages/api/src/index.ts to handle WS upgrade outside Hono per the PRD's chicken-and-egg note: top-level fetch(req, server) inspects new URL(req.url).pathname — when it's "/sync/ws" it pulls ?token=, matches against getEnv().API_SECRET (returns plain Response("unauthorized", 401) on miss — browsers can't set Authorization headers on WS, query-string token is documented in PRD risks), then calls server.upgrade(req, { data: {started:false, cancelled:false} satisfies SyncSocketData }), returning undefined on success or Response("upgrade failed", 400) on failure. All other paths delegate to app.fetch(req). websocket handlers: open=noop (wait for sync.start), message=void handleSyncMessage(ws, raw), close=mark ws.data.cancelled=true (no abort plumbing in v1 per PRD). Bun.serve<SyncSocketData>({...}) carries the data type into ServerWebSocket<SyncSocketData> so handleSyncMessage's ws.data is fully typed. Type wrinkle: @miel/core re-exports syncEventSchemas as a namespace (zod schemas) but not the inferred TS types, so I derived `type SyncServerEventT = z.infer<typeof syncEventSchemas.SyncServerEvent>` inside syncSocket.ts rather than punching a new export through the core barrel (keeps the change scoped to step 4). bun run typecheck → 5 cache misses, all green in 1.2s. Smoke test (packages/api/scripts/smoke-ws.ts, against the live `bun --watch` dev server on :3001 which auto-reloaded after the file writes): (1) `new WebSocket('ws://localhost:3001/sync/ws?token=wrong')` closes without opening — bad token rejected at upgrade time, no upgrade attempt visible to the WS layer; (2) connect with the right token + send the literal string "this is not json" → receives `{type:'sync.error', message:'invalid JSON: JSON Parse error: Unexpected identifier "this"'}` then close; (3) send `{type:'not.a.real.event'}` → receives `{type:'sync.error', message:'invalid sync.start payload: [{received:"not.a.real.event", code:"invalid_literal", expected:"sync.start", path:["type"]}]'}` then close; (4) send `{type:'sync.start', account:'no-such-account@example.com', since:'1d'}` → receives `sync.started` then `sync.finished` (syncAll with an unknown account email returns an empty runs[] rather than throwing, because syncAccountsFromGog runs first and the email filter just yields zero accounts to sync — confirming the full success path through syncAll, the onEvent passthrough, and the trailing sync.finished). curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/sync/ws → 401 (plain GET without token is correctly rejected at the upgrade gate). wscat isn't installed locally so the smoke script uses Bun's built-in WebSocket client instead — same protocol, same observable behavior. Live event streaming during a real fetchAndTriage run (triage.started/batch.progress/filters.started/triage.finished/filters.finished) is exercised end-to-end by Steps 2-3's CLI smoke + the upcoming Step 5 frontend integration; this step only had to verify the WS upgrade plumbing + the message routing into syncAll, both of which are confirmed. Steps 5 (useSyncStream hook + SyncRangeControls swap) and 6 (delete SyncStatusBanner / POST /sync / useSync / SyncResponse + LayoutContext trim) remain.
2026-05-21  WS sync streaming Step 5 (useSyncStream hook + SyncRangeControls swap): wrote packages/web/src/api/syncSocket.ts exposing `useSyncStream()` → `{start, isRunning}`. The hook opens a WebSocket to /api/sync/ws?token=<apiSecret>, sends `{type:"sync.start", ...input}` on open, validates each incoming frame with syncEventSchemas.SyncServerEvent.safeParse, and dispatches into a switch that maps event types onto sonner toasts + react-query invalidations per the PRD's event→toast table. Toast IDs are scoped per-account (`sync:triage:${account}`, `sync:filters:${account}`) so multi-account runs don't clobber each other; batch counters live in a per-instance `useRef<Map<string, {done, failed}>>` keyed by account so each triage.batch.progress event updates the loading-toast description to `${completed}/${totalBatches} batches` (plus a `(N failed)` suffix when any batch failed). Side effects per the table: mails.fetched invalidates ["messages"] so the inbox's "Not yet triaged" section populates immediately + emits an info toast (suppressed when count===0 to avoid noise on empty syncs); triage.finished invalidates ["messages"] + accounts and surfaces a success toast with the per-account totals; filters.finished invalidates ["filters"] + emits a success toast when suggestedFilters>0; sync.finished does a final accounts invalidate; sync.error emits an error toast. WS URL builder picks `wss:` when the page is `https:` (browsers can't set Authorization on WS, so the bearer token rides in `?token=` per Step 4); when apiBase is a path like "/api" it composes with window.location.origin. The hook cleans up on unmount via useEffect return → ws.close(); a new start() while a socket is alive closes the previous one first, so double-clicks don't fan out parallel runs. Type wrinkle: @miel/web doesn't depend on zod directly, so I derived the union type via `ReturnType<typeof syncEventSchemas.SyncServerEvent.parse>` instead of `z.infer`. Wired `SyncRangeControls.tsx` to the new hook per PRD step 5: replaced `useSync()` with `useSyncStream()`, `sync.mutate(...)` with `start(...)`, `sync.isPending` with `isRunning`; left the legacy `onResult`/`onError` props on the Props interface (annotated as legacy) so InboxPage + MessageDetailPage still typecheck — Step 6 will trim those callers along with deleting SyncStatusBanner / useSync / SyncResponse. Build wrinkle: this is the first @miel/web file to import from @miel/core, and the core barrel transitively pulls in postgres+drizzle through services/* → db/client.ts, which exploded vite's rollup build (`"performance" is not exported by "__vite-browser-external"`). Fix: added a sub-path export to packages/core/package.json — `"./schemas/syncEvents": "./src/schemas/syncEvents.ts"` — so the hook can deep-import via `import * as syncEventSchemas from "@miel/core/schemas/syncEvents"`, bypassing the side-effectful barrel. Verified: bun run typecheck → 5 cache misses (core/api/cli/web), all green in 2.5s; cd packages/web && bun run build → vite production build 2362 modules transformed (up from 423 — drizzle/postgres tree-shake remnants pulled in by other code paths in node_modules, expected), 19.65 kB CSS / 523.82 kB JS gzip 153.44 kB. Live browser exercise of the WS toast flow deferred to interactive testing — Step 4's wscat-equivalent script confirmed the WS upgrade + event stream on the server side; the frontend hook only translates those events into toast/invalidation calls. Step 6 (delete SyncStatusBanner, POST /sync route, useSync/SyncResponse, trim LayoutContext + InboxPage + MessageDetailPage + SettingsSyncTrigger) is the remaining work.
2026-05-21  WS sync streaming Step 6 (cleanup of legacy HTTP sync surface): deleted packages/web/src/features/sync/SyncStatusBanner.tsx and packages/api/src/routes/sync.ts (the POST /sync HTTP route). Unwired both: removed `import { syncRoutes }` and `app.route("/sync", syncRoutes)` from packages/api/src/app.ts (the WS upgrade in src/index.ts handles /sync/ws directly and is unaffected). Trimmed LayoutContext in packages/web/src/App.tsx — removed the `SyncResultInfo` interface, the `SyncStatus` union type, the `syncStatus` state + `setSyncStatus`, and all four LayoutContext fields (`syncStatus`, `onSyncResult`, `onSyncError`, `dismissSyncStatus`); trimmed the `useMemo` deps accordingly. All three pages that consumed those LayoutContext fields cleaned up: InboxPage.tsx, MessageDetailPage.tsx, and FiltersPage.tsx (FiltersPage wasn't named in the PRD's "Modify" list but it consumes the same LayoutContext fields and would have failed typecheck — added it to the cleanup). Each: dropped the `SyncStatusBanner` import + JSX, the destructured `syncStatus/onSyncResult/onSyncError/dismissSyncStatus` fields, and the `onResult/onError` props passed to `<SyncRangeControls>`. Dropped the legacy `onResult`/`onError` props from SyncRangeControls.tsx's Props interface (left from Step 5's transitional state). Rewrote SettingsSyncTrigger.tsx to use `useSyncStream` instead of `useSync` — calls `start({account, since})` directly, exposes `isRunning` for the button spinner, drops the per-run summary table + the `runs` state + the inline `sync.isError` reauth-aware error display (toasts in the dispatcher cover both the success summary and error surface now). Removed `useSync` + `SyncInput` from packages/web/src/api/mutations.ts along with the `maybeShowReauthToast` import + the `SyncResponse` type import. Removed `SyncResponse` interface from packages/web/src/api/types.ts (kept `SyncRunResult` per PRD — still used by the `sync.finished` event payload that flows through the WS dispatcher). Left packages/web/src/features/auth/reauthToast.tsx in place: `maybeShowReauthToast` is still exported and the helper is fully self-contained (the WS error path doesn't currently surface reauth_required errors — the HTTP API for label/reply mutations still does, and other callers may pick this up). Left @miel/core's `apiSchemas.SyncRequest` in place per PRD ("Keep `apiSchemas.SyncRequest` — it stays useful as the payload of `sync.start`"). Verified: bun run typecheck → 2 cache misses (api + web; core + cli cache hits), all 5 tasks green in 1.3s; cd packages/web && bun run build → vite production build 2178 modules transformed (down from 2362 in Step 5 — fewer modules pulled in now that useSync's import chain through mutations.ts is gone), 19.12 kB CSS / 519.92 kB JS gzip 152.22 kB. PRD's "Implementation order" section is now fully checked off (Steps 1-6 all ✅ Done 2026-05-21). The full WebSocket sync streaming feature is complete.
