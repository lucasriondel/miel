services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: miel
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-miel}
      POSTGRES_DB: miel
    ports:
      - "5435:5432"
    volumes:
      - miel-pgdata:/var/lib/postgresql/data
volumes:
  miel-pgdata:
